const mongoose = require("mongoose")
const {Restarent schema } = mongoose
const { Restarent schema: schemaTypes } = mongoose.Schema


const {name}:
            type:string,
            required=true,

const {city type}:
            type:string,
            require=true,

const {dish type}:
            type:string,
            require=true,

const {cost type}:
            type:Number,
            require=true,

        

         